[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ezdata-cli"
version = "2.0.0"
description = "ezdata backend internal CLI entrypoint"
requires-python = ">=3.10"

[project.scripts]
ruoyi = "cli.main:main"

[tool.setuptools.packages.find]
include = ["cli*"]
